---
title: "Fiction"
layout: layouts/books.njk
---

<h1>Books</h1>

<ul class="books">
{% for post in collections['books-info'] | head(-5, true) %}
<li>
	<img src="{{ post.data.coverImage }}" />
	<p><a href="{{ post.url }}">{{ post.data.title }}</a></p>
	<p><strong>{{ post.data.publication }}</strong></p>
	<p>{{ post.date | publicationDate }}</p>
</li>
{% endfor %}
</ul>

<p><a href="./books/">See more...</a></p>

<img width="125" class="feather-separator" src="/img/svg/feathers.svg" />

<h1>Short Stories</h1>

<ul class="books">
{% for post in collections['short-stories-info'] | head(-5, true) %}
<li>
	<img src="{{ post.data.coverImage }}" />
	<p><a href="{{ post.url }}">{{ post.data.title }}</a></p>
	<p><strong>{{ post.data.publication }}</strong></p>
	<p>{{ post.date | publicationDate }}</p>
</li>
{% endfor %}
</ul>

<p><a href="./short-stories/">See more...</a></p>

<img width="125" class="feather-separator" src="/img/svg/feathers.svg" />

<h1>Anthologies</h1>
<ul class="books">
{% for post in collections['anthologies-info'] | head(-5, true) %}
<li>
	<img src="{{ post.data.coverImage }}" />
	<p><a href="{{ post.url }}">{{ post.data.title }}</a></p>
	<p><strong>{{ post.data.publication }}</strong></p>
	<p>{{ post.date | publicationDate }}</p>
</li>
{% endfor %}
</ul>

<p><a href="./anthologies/">See more...</a></p>
